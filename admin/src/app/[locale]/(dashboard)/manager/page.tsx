import ManagerScreen from "@/components/screens/dashboardScreen/manager";

import React from "react";

type Props = {};

const page = (props: Props) => {
  return (
    <div>
      <ManagerScreen />
    </div>
  );
};

export default page;
